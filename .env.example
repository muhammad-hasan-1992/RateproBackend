# ========================================
# RatePro Backend — Environment Variables
# ========================================
# Copy this file to .env and fill in your values.
# NEVER commit the actual .env file to version control.

# ⚙️ Infrastructure (NEVER admin-editable)
# ----------------------------------------
NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://localhost:27017/ratepro

# JWT & Auth
JWT_SECRET=your_jwt_secret_here
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here
JWT_EXPIRE=30m

# Config Encryption (32 bytes = 64 hex chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CONFIG_ENCRYPTION_KEY=

# Redis (for BullMQ queues)
REDIS_URL=redis://localhost:6379

# 🌐 URLs
# ----------------------------------------
PUBLIC_URL_LOCAL=http://localhost:5173
ADMIN_URL_LOCAL=http://localhost:5174
PUBLIC_URL_PROD=
ADMIN_URL_PROD=

# ☁️ Cloudinary (Infrastructure — file storage)
# ----------------------------------------
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# 📧 Email (Business key — migrate to DB via admin panel)
# ----------------------------------------
SENDGRID_API_KEY=
FROM_NAME=RatePro
FROM_EMAIL=noreply@ratepro.com

# 📱 SMS (Business key — migrate to DB via admin panel)
# ----------------------------------------
SMS_PROVIDER_SID=
SMS_PROVIDER_AUTH_TOKEN=
SMS_PROVIDER_NUMBER=

# 💬 WhatsApp (Business key — migrate to DB via admin panel)
# ----------------------------------------
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
META_WHATSAPP_PHONE_NUMBER_ID=
META_WHATSAPP_TOKEN=

# 🤖 AI (Business key — migrate to DB via admin panel)
# ----------------------------------------
GEMINI_API_KEY=

# 🔧 Feature Flags
# ----------------------------------------
ENABLE_QUEUES=false
